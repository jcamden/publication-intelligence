{
	"name": "@pubint/pdf",
	"version": "0.0.0",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"import": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc",
		"lint:biome": "biome ci .",
		"clean": "rm -rf dist",
		"typecheck": "tsc --noEmit",
		"test": "pnpm test:unit",
		"test:unit": "mkdir -p test-results && vitest run; VITEST_EXIT=$?; pnpx tsx ../../scripts/format-unit-test-results.ts test-results/unit-tests.json test-results/unit-summary.md; exit $VITEST_EXIT",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"@pubint/core": "workspace:*"
	},
	"devDependencies": {
		"@pubint/vitest-config": "workspace:*",
		"@types/node": "^22.10.5",
		"typescript": "^5.7.3",
		"vitest": "^4.0.18"
	}
}
