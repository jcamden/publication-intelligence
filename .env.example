# Application
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# CORS Allowed Origins (comma-separated)
# For production, set to your actual frontend domains
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:6006

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/publication_intelligence

# JWT Authentication
# CRITICAL: Generate a secure random key for production
# Generate with: openssl rand -base64 32
# The value below is safe for local development only - DO NOT use in production!
JWT_SECRET=dev-secret-key-for-local-development-only-not-for-production
JWT_EXPIRES_IN=7d
