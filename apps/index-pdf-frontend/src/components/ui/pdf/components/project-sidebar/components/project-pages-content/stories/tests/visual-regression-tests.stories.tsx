import {
	defaultGlobals,
	visualRegressionTestConfig,
} from "@pubint/storybook-config";
import type { Meta, StoryObj } from "@storybook/react";
import { ProjectPagesContent } from "../../project-pages-content";

const meta: Meta<typeof ProjectPagesContent> = {
	title:
		"Components/PDF/PdfEditor/ProjectSidebar/ProjectPagesContent/tests/Visual Regression Tests",
	component: ProjectPagesContent,
	parameters: {
		...visualRegressionTestConfig,
		layout: "centered",
	},
	tags: ["visual-regression"],
};

export default meta;
type Story = StoryObj<typeof meta>;

export const LightMode: Story = {
	globals: {
		...defaultGlobals,
	},
};

export const DarkMode: Story = {
	globals: {
		...defaultGlobals,
		theme: "dark",
	},
};
