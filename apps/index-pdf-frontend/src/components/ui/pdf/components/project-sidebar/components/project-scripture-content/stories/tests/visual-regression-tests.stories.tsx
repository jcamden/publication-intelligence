import {
	defaultGlobals,
	visualRegressionTestConfig,
} from "@pubint/storybook-config";
import type { Meta, StoryObj } from "@storybook/react";
import { ProjectScriptureContent } from "../../project-scripture-content";

const meta: Meta<typeof ProjectScriptureContent> = {
	title:
		"Components/PDF/PdfEditor/ProjectSidebar/ProjectScriptureContent/tests/Visual Regression Tests",
	component: ProjectScriptureContent,
	parameters: {
		...visualRegressionTestConfig,
		layout: "centered",
	},
	tags: ["visual-regression"],
};

export default meta;
type Story = StoryObj<typeof meta>;

export const LightMode: Story = {
	globals: {
		...defaultGlobals,
	},
};

export const DarkMode: Story = {
	globals: {
		...defaultGlobals,
		theme: "dark",
	},
};
