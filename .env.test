# Test Environment Configuration
# Tests use PGLite (in-memory) - no external database needed!

# Test-specific settings
NODE_ENV=test
LOG_LEVEL=error

# JWT Authentication
JWT_SECRET=test-secret-key-for-local-development-only
JWT_EXPIRES_IN=7d