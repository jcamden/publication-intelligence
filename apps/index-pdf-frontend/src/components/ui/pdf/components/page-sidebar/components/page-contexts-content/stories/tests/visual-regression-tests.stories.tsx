import {
	defaultGlobals,
	visualRegressionTestConfig,
} from "@pubint/storybook-config";
import type { Meta, StoryObj } from "@storybook/react";
import { PageContextsContent } from "../../page-contexts-content";

const meta: Meta<typeof PageContextsContent> = {
	title:
		"Components/PDF/PdfEditor/PageSidebar/PageContextsContent/tests/Visual Regression Tests",
	component: PageContextsContent,
	parameters: {
		...visualRegressionTestConfig,
		layout: "centered",
	},
	tags: ["visual-regression"],
};

export default meta;
type Story = StoryObj<typeof meta>;

export const LightMode: Story = {
	globals: {
		...defaultGlobals,
	},
};

export const DarkMode: Story = {
	globals: {
		...defaultGlobals,
		theme: "dark",
	},
};
