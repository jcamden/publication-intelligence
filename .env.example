# EdgeDB/Gel Auth Signing Key
# CRITICAL: Generate a secure random key for production
# Generate with: openssl rand -base64 32
# Minimum 32 bytes required
EDGEDB_AUTH_SIGNING_KEY=REPLACE_WITH_SECURE_KEY_IN_PRODUCTION

# Application
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Gel/EdgeDB Auth URL
GEL_AUTH_URL=http://localhost:10701/db/main/ext/auth

# CORS Allowed Origins (comma-separated)
# For production, set to your actual frontend domains
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:6006

# Gel/EdgeDB Backup Configuration
# Disable automatic backups during migrations (saves time/space in dev)
# For production, set to 'enabled' or remove this line
GEL_AUTO_BACKUP_MODE=disabled
